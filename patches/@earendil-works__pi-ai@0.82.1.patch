diff --git a/dist/api/openai-completions.js b/dist/api/openai-completions.js
index ed81baa3860fa20cf9d38885f9dd4b6b2f639045..c936ffff29adf0e8422c179727aef4cb73c72b14 100644
--- a/dist/api/openai-completions.js
+++ b/dist/api/openai-completions.js
@@ -814,6 +814,29 @@ export function convertMessages(model, context, compat, options) {
                             text: sanitizeSurrogates(item.text),
                         };
                     }
+                    else if (item.dshVideo === true) {
+                        // A promoted URL keeps the request body at a few
+                        // hundred bytes; inline base64 is 4/3 the file and a
+                        // gateway rejects an oversized body outright.
+                        if (typeof item.dshVideoUrl === "string" && item.dshVideoUrl.length > 0) {
+                            return {
+                                type: "video_url",
+                                video_url: { url: item.dshVideoUrl },
+                            };
+                        }
+                        // Harness video marker. UserMessage.content is typed
+                        // (TextContent | ImageContent)[], so without this branch a
+                        // video falls through to image_url and the provider rejects
+                        // it outright ("The image format is illegal and cannot be
+                        // opened"). Absent the marker this file behaves exactly as
+                        // published, so a request carrying no video is unchanged.
+                        return {
+                            type: "video_url",
+                            video_url: {
+                                url: `data:${item.mimeType};base64,${item.data}`,
+                            },
+                        };
+                    }
                     else {
                         return {
                             type: "image_url",
@@ -969,15 +992,24 @@ export function convertMessages(model, context, compat, options) {
                         deferredToolNames.add(name);
                     }
                 }
-                if (hasImages && model.input.includes("image")) {
+                if (hasImages && (model.input.includes("image") || model.input.includes("video"))) {
                     for (const block of toolMsg.content) {
                         if (isImageContentBlock(block)) {
-                            imageBlocks.push({
-                                type: "image_url",
-                                image_url: {
-                                    url: `data:${block.mimeType};base64,${block.data}`,
-                                },
-                            });
+                            imageBlocks.push(block.dshVideo === true
+                                ? {
+                                    type: "video_url",
+                                    video_url: {
+                                        url: typeof block.dshVideoUrl === "string" && block.dshVideoUrl.length > 0
+                                            ? block.dshVideoUrl
+                                            : `data:${block.mimeType};base64,${block.data}`,
+                                    },
+                                }
+                                : {
+                                    type: "image_url",
+                                    image_url: {
+                                        url: `data:${block.mimeType};base64,${block.data}`,
+                                    },
+                                });
                         }
                     }
                 }
diff --git a/dist/api/transform-messages.js b/dist/api/transform-messages.js
index 6aa6aa2bb660194e64a06d5dc98f61862311f210..6faf7e81aa34272cc0980b2d32fb0b8e4211fd45 100644
--- a/dist/api/transform-messages.js
+++ b/dist/api/transform-messages.js
@@ -4,7 +4,10 @@ function replaceImagesWithPlaceholder(content, placeholder) {
     const result = [];
     let previousWasPlaceholder = false;
     for (const block of content) {
-        if (block.type === "image") {
+        // A harness video marker is image-typed so it can ride
+        // (TextContent | ImageContent)[]; it must not be downgraded by the
+        // image capability check, which is gated on "image" alone.
+        if (block.type === "image" && block.dshVideo !== true) {
             if (!previousWasPlaceholder) {
                 result.push({ type: "text", text: placeholder });
             }
@@ -17,7 +20,7 @@ function replaceImagesWithPlaceholder(content, placeholder) {
     return result;
 }
 function downgradeUnsupportedImages(messages, model) {
-    if (model.input.includes("image")) {
+    if (model.input.includes("image") || model.input.includes("video")) {
         return messages;
     }
     return messages.map((msg) => {
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 0a1b76919766126f1c8c4a306b24e8096a5e02d0..c462d2a1bb32a595280582f32d7bf6baf3cbbba7 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -240,6 +240,20 @@ export interface ImageContent {
     type: "image";
     data: string;
     mimeType: string;
+    /**
+     * DSH patch: marks an entry the OpenAI-completions serializer must emit as
+     * a `video_url` part. `UserMessage.content` admits only text and image, and
+     * an mp4 sent as `image_url` is rejected by the provider outright, so a
+     * video needs a discriminator that travels in the same ordered array.
+     */
+    dshVideo?: boolean;
+    /**
+     * DSH patch: a provider-fetchable URL for a `dshVideo` entry. Present
+     * means `data` is empty and this address is emitted instead of a base64
+     * data URI, which keeps the request body small enough to clear a
+     * gateway's body-size limit.
+     */
+    dshVideoUrl?: string;
 }
 export interface ToolCall {
     type: "toolCall";
@@ -646,7 +660,7 @@ export interface Model<TApi extends Api> {
      * Missing keys use provider defaults. null marks a level as unsupported.
      */
     thinkingLevelMap?: ThinkingLevelMap;
-    input: ("text" | "image")[];
+    input: ("text" | "image" | "video")[];
     cost: ModelCost;
     contextWindow: number;
     maxTokens: number;
